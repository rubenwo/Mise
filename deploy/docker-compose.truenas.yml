services:
  recipes:
    image: ghcr.io/rubenwo/recipes:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /mnt/tank/recipes/config.yaml:/app/config.yaml:ro
      - /mnt/tank/recipes/images:/app/images
