services:
  recipes:
    image: ghcr.io/rubenwo/recipes:latest
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - /mnt/Storage/NAS/Eten/config.yaml:/app/config.yaml:ro
      - /mnt/Storage/NAS/Eten/images:/app/images
